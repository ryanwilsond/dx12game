
public static class Program {
    public static void Main() {
        pinned var engine = new Engine();

        if (engine.Initialize()) {
            while (engine.IsRunning())
                engine.Update();
        }
    }
}
